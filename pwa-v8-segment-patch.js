// Namibia PWA v8 segment-level driving guidance patch.
// Loaded after app.js. Mutates existing DATA and overrides selected render functions.
(function () {
  const DRIVE = {
    1:{summary:'Airport-to-city transfer followed by Windhoek urban errands.',expect:['Mostly paved highway from Hosea Kutako into Windhoek, then city driving.','The fiddly part is inside Windhoek: urban turns, parking, food choice, grocery stop, and hotel approach.'],hazards:['Left-side adaptation','Urban intersections','Post-flight fatigue']},
    2:{summary:'Windhoek to the Namib Desert via long gravel/corrugated sections and open country.',expect:['After leaving Windhoek, expect long open-road stretches and slower gravel/corrugated driving.','Solitaire is the key planned service stop for fuel, bathrooms, coffee, and tyre-pressure check.'],hazards:['Gravel/corrugations','Sparse services','Speed discipline']},
    3:{summary:'Early desert park drive with a sand-section decision near Sossusvlei/Deadvlei.',expect:['Very early departure in low light; reach Sesriem gate calmly before opening.','If self-driving the final sand section, deflate before sand and reinflate before faster road driving.'],hazards:['Soft/deep sand','Heat exposure','Tourist traffic','False confidence from 4x4']},
    4:{summary:'Short but early logistics-heavy desert driving around Sesriem/Namib Sky/Dune Star.',expect:['Pre-dawn local driving to the balloon meeting point, then short logistics hops after the flight.','The challenge is timing and confirmations more than distance.'],hazards:['Pre-dawn driving','Meeting-time precision','Lodge access tracks']},
    5:{summary:'Long desert-to-coast crossing through Solitaire and the C14 corridor.',expect:['Remote-feeling driving day with sustained gravel/desert-road character before reaching the coast.','At Walvis Bay/Swakopmund, reset to highway/tar pressure before paved coastal roads.'],hazards:['Long remote sections','Fatigue','Corrugations','Over-speeding temptation']},
    6:{summary:'Short paved coastal drive to Walvis Bay; tour operator handles the off-road dune portion.',expect:['Easy paved coastal road between Swakopmund and Walvis Bay.','The demanding sand/dune driving belongs to the guided Mola Mola tour vehicle.'],hazards:['Coastal traffic','Fog/low visibility','Parking/check-in timing']},
    7:{summary:'Coast-to-inland transition after kayaking, then Spitzkoppe access roads.',expect:['Morning coastal drive to Walvis Bay, then a longer inland run after kayaking.','Usakos is the practical point to fuel and reset tyre pressure before leaving the main corridor.'],hazards:['Post-activity fatigue','Afternoon inland driving','Final local access roads']},
    8:{summary:'Local scenic driving/walking around Spitzkoppe; route depends on lodge guidance.',expect:['No fixed long-distance route.','Expect local tracks, scenic stops, and lodge-specific access guidance.'],hazards:['Unmarked tracks','Sharp rocks','Low-speed puncture risk','Heat']},
    9:{summary:'Long northbound repositioning day from Spitzkoppe area to Etosha King Nehale.',expect:['Transition from gravel/local access back to faster tar/highway sections.','Otjiwarongo is the major refuel/check point; Omuthiya/Oshivelo corridor is the prudent final top-up.'],hazards:['Long duration','Sunday service uncertainty','Fatigue','Late-arrival risk']},
    10:{summary:'Guided Etosha game-drive day; no independent route planning.',expect:['You are not controlling the driving route.','Expect cold early-morning conditions, wildlife-dependent routing, and long periods in a safari vehicle.'],hazards:['Cold morning','Dust','Wildlife proximity']},
    11:{summary:'Second guided Etosha game-drive day.',expect:['Route is guide- and wildlife-dependent.','Use lodge confirmations for exact morning and afternoon departure times.'],hazards:['Cold morning','Dust','Wildlife proximity','Late return']},
    12:{summary:'Long southbound return from Etosha King Nehale to Windhoek.',expect:['Another long repositioning day about endurance, fuel discipline, and buffer time.','Fuel strategy assumes you wait until Otjiwarongo if the gauge supports it; use earlier options if the actual gauge differs.'],hazards:['Long duration','Fatigue','Lunch uncertainty','Paved-road overconfidence']},
    13:{summary:'Short Windhoek-to-airport departure drive with rental return logistics.',expect:['Urban departure plus paved airport road.','Craft Centre is optional and should not compromise airport/rental-return buffer.'],hazards:['Time pressure','Urban traffic','Rental return delays','Final fuel']}
  };
  // HEATHER segment data — calibrated against per-route Namibia road research
  // (sources: Expert Africa, Tracks4Africa, Dangerous Roads, Namibia Bookings).
  // Spreetshoogte (C24) and Bosua (C28) are explicitly David-only at 14–20%
  // gradient; C14 corrugations require gravel confidence; B1/B2/B6 are fully
  // paved and well within a cautious driver's range. The 60 km Sesriem→2x4
  // park road is paved and is actually Heather's best driving opportunity.
  const HEATHER = {
    1:[{status:'can_drive',label:'Heather drives: B6 airport road end-to-end',from:'Hosea Kutako International Airport',to:'Windhoek hotel approach',reason:'B6 / Hage Geingob Freeway is fully paved (~45 km), well-signed, light morning traffic. Ideal warm-up. David takes city errands + parking only.'}],
    2:[{status:'partial',label:'Heather: B1 Windhoek → Rehoboth only',from:'Windhoek',to:'Rehoboth',reason:'B1 paved trunk highway for the first ~90 km is comfortable. Hand the wheel at or before Rehoboth — the C24 gravel + Spreetshoogte Pass (14% gradient, 1,000 m drop in 4 km, no guardrails) is firmly David-only.'}],
    3:[{status:'can_drive',label:"Heather: Sesriem → 2x4 car park (paved park road)",from:'Sesriem gate',to:'2x4 car park (before sand section)',reason:"~60 km of fully tarred straight park road, light traffic, 60 km/h park speed limit. One of Heather's best driving opportunities of the whole trip. David takes the final 5 km deep-sand track to Deadvlei (or use the About Africa shuttle)."}],
    4:[],  // Namib Dune Star Camp is lodge transfer only — no self-drive
    5:[],  // C14 Solitaire → Walvis Bay is gorge-edge corrugated gravel, David only
    6:[{status:'can_drive',label:'Heather: B2 Swakopmund ↔ Walvis Bay both ways',from:'Fritz Manor (Swakopmund)',to:'Walvis Bay Waterfront and back',reason:"Fully paved B2 coastal corridor, ~30 km each way, flat. The easiest driving day of the trip and a good seat-time builder."}],
    7:[{status:'partial',label:'Heather: B2 paved + well-graded D1918 start',from:'Walvis Bay area',to:'D1918 first ~13 km from Usakos turnoff',reason:'B2 to the Usakos turnoff is straightforward. The well-graded first portion of D1918 at reduced speed (<60 km/h) is manageable; David takes over once corrugations begin in the final ~10 km before the gate, and the rough lodge access track.'}],
    8:[],  // Local Spitzkoppe tracks — rocky/sandy, low-speed 4x4 judgment required
    9:[{status:'can_drive',label:'Heather: ~550 km of paved B2/B1 highway',from:'Spitzkoppe',to:'King Nehale Gate spur',reason:'Long mostly-highway day. B2 → Okahandja → B1 north through Otjiwarongo and Tsumeb is fully paved. Heather handles the bulk. David takes the final 5 km C3646 gravel spur to the gate (or Heather at 30 km/h).'}],
    10:[],  // Guided game drives
    11:[],  // Guided game drives
    12:[{status:'can_drive',label:'Heather: substantial B1 south stretches',from:'B1 south paved sections',to:'Windhoek approach outskirts',reason:'Long ~600 km B1 paved highway day. Recommend sharing in 2-hour blocks with breaks. David handles fuel timing decisions and any unexpected detours.'}],
    13:[{status:'can_drive',label:'Heather: B6 airport drive end-to-end',from:'Windhoek',to:'Hosea Kutako International Airport',reason:'B6 fully paved, ~45 km, very light early-morning traffic. Simplest driving day. David handles only final rental-return logistics at the airport.'}]
  };
  for (const d of DATA.days) { d.driveExperience = DRIVE[d.day] || {summary:'',expect:[],hazards:[]}; delete d.heatherDrive; d.heatherDriveSegments = HEATHER[d.day] || []; }

  function heatherSummary(d){ const s=d.heatherDriveSegments||[]; if(!s.length) return 'David drives'; return s.some(x=>x.status==='can_drive')?'Heather OK segment':'Heather maybe segment'; }
  function heatherBadge(d){ const s=d.heatherDriveSegments||[]; if(!s.length) return ''; return `<div class="heather-segments">${s.map(seg=>{const icon=seg.status==='can_drive'?'✅':'⚠️';return `<div class="heather-segment ${seg.status}"><div class="heather-badge ${seg.status}">${icon} ${esc(seg.label)}</div><div class="heather-note"><strong>${esc(seg.from)} → ${esc(seg.to)}</strong><br>${esc(seg.reason)}</div></div>`}).join('')}</div>`; }
  function heatherPrintBlock(d){ const s=d.heatherDriveSegments||[]; if(!s.length) return ''; return `<div class="print-heather segment-level"><h3>Heather driving segments</h3>${s.map(seg=>{const icon=seg.status==='can_drive'?'✅':'⚠️';return `<p><strong>${icon} ${esc(seg.label)}</strong><br>${esc(seg.from)} → ${esc(seg.to)}<br>${esc(seg.reason)}</p>`}).join('')}<p><strong>Rule:</strong> No Heather segment badge means David should own that leg/section.</p></div>`; }
  function projectRoutePoints(d){ const cached=state.renderedRoutes[d.date]?.overviewPath; const routePts=cached?.length?cached:routeStops(d).map(s=>({lat:s.lat,lng:s.lng})); const pins=d.stops.map(s=>({...s,lat:+s.lat,lng:+s.lng})); const pts=routePts.concat(pins.map(p=>({lat:p.lat,lng:p.lng}))); if(!pts.length)return{route:[],pins:[],source:'none'}; let minLat=Math.min(...pts.map(p=>p.lat)),maxLat=Math.max(...pts.map(p=>p.lat)),minLng=Math.min(...pts.map(p=>p.lng)),maxLng=Math.max(...pts.map(p=>p.lng)); if(maxLat-minLat<.01){maxLat+=.005;minLat-=.005} if(maxLng-minLng<.01){maxLng+=.005;minLng-=.005} const pad=18,w=1000,h=360; const proj=p=>({x:pad+((p.lng-minLng)/(maxLng-minLng))*(w-2*pad),y:pad+((maxLat-p.lat)/(maxLat-minLat))*(h-2*pad)}); return{route:routePts.map(proj),pins:pins.map(p=>({...p,...proj(p)})),source:cached?.length?'Google Directions cached route geometry':'fallback mandatory-stop route sketch'}; }
  function routeSketchSvg(d,opts={}){ const p=projectRoutePoints(d); if(!p.route.length&&!p.pins.length)return '<div class="route-sketch"><div class="map-fallback">No mapped route for this day.</div></div>'; const line=p.route.length?p.route.map((pt,i)=>`${i?'L':'M'}${pt.x.toFixed(1)},${pt.y.toFixed(1)}`).join(' '):''; const pins=p.pins.map((s,i)=>{const cls=s.routeRole==='optional'?'optional':(s.routeRole==='mandatoryAction'?'mandatoryAction':'mandatory'); const label=s.routeRole==='optional'?'O':(s.routeRole==='mandatoryAction'?'!':String(i+1)); const text=opts.print?'':`<text class="route-pin-label" x="${s.x+12}" y="${s.y-10}">${esc((s.emoji||'')+' '+s.time)}</text>`; return `<g><circle class="route-pin ${cls}" cx="${s.x.toFixed(1)}" cy="${s.y.toFixed(1)}" r="${opts.print?7:9}"></circle><text class="route-pin-label" x="${s.x-3.8}" y="${s.y+4}">${label}</text>${text}</g>`}).join(''); return `<div class="${opts.print?'print-map':'route-sketch'}"><svg viewBox="0 0 1000 360" role="img" aria-label="Route sketch for ${esc(d.title)}"><defs><pattern id="sandPattern${d.day}" width="36" height="36" patternUnits="userSpaceOnUse"><path d="M0 25 C10 15 22 35 36 20" fill="none" stroke="rgba(217,139,66,.18)" stroke-width="2"/></pattern></defs><rect x="0" y="0" width="1000" height="360" fill="url(#sandPattern${d.day})"/>${line?`<path class="route-line ${p.source.startsWith('fallback')?'route-line-fallback':''}" d="${line}"/>`:''}${pins}</svg></div>${opts.print?'':`<div class="route-map-caption">Map sketch source: ${esc(p.source)}. Mandatory route line drawn; optional pins are shown but not routed.</div>`}`; }

  render = function(){ const d=day(); const next=d.date===todayISO()?(d.stops.find(s=>{const m=parseTimeMinutes(s.time);return m!==null&&m>=nowMinutes()})||d.stops[d.stops.length-1]):d.stops[0]; let gpsLine='GPS not enabled.'; if(state.gps){const nearest=d.stops.map(s=>({s,m:distMeters(state.gps,s)})).sort((a,b)=>a.m-b.m)[0]; gpsLine=nearest?`Nearest stop: ${nearest.s.emoji} ${nearest.s.name} (${Math.round(nearest.m)}m)`:'GPS enabled.'} $('dashboard').innerHTML=`<div class="now-date">Day ${d.day} · ${d.date}</div><div class="now-title">${esc(d.title)}</div><div class="kv"><div><strong>Next</strong><span>${next?`${next.emoji} ${esc(next.time)} — ${esc(next.name)}`:'—'}</span></div><div><strong>Route</strong><span>${d.selfDrive?`${routeStops(d).length} mandatory route stops`:'No fixed self-drive route'}</span></div><div><strong>Heather</strong><span>${esc(heatherSummary(d))}</span></div><div><strong>GPS</strong><span>${esc(gpsLine)}</span></div></div><div class="hero-actions" style="justify-content:flex-start;margin-top:12px">${next?`<a href="${wazeUrl(next)}" target="_blank"><button class="primary">Open next in Waze</button></a>`:''}${googleMapsUrl(d)?`<a href="${googleMapsUrl(d)}" target="_blank"><button>Google route</button></a>`:''}</div>`; renderMapMarkers(); renderTab(); renderPrintPages(); };

  renderTab = function(){ document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t.dataset.tab===state.activeTab)); const d=day(); const route=state.renderedRoutes[d.date]; if(state.activeTab==='overview'){const pc=d.stops.filter(s=>s.pressure).length,fc=d.stops.filter(s=>s.fuel).length; $('tabContent').innerHTML=`<div class="panel-title" style="padding:0 0 14px"><div><h2>${esc(d.title)}</h2><p>${esc(d.routeNotes)}</p></div></div>${routeSketchSvg(d)}<div class="route-summary"><div class="metric"><b>${d.selfDrive?'Yes':'No'}</b><span>self-drive route</span></div><div class="metric"><b>${pc}</b><span>pressure actions</span></div><div class="metric"><b>${fc}</b><span>fuel estimates</span></div></div><div class="alert green"><strong>What to expect:</strong> ${esc(d.driveExperience?.summary||'')}</div>${heatherBadge(d)}<ul class="experience-list">${(d.driveExperience?.expect||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="hazard-list">${(d.driveExperience?.hazards||[]).map(x=>`<span class="badge pressure">${esc(x)}</span>`).join('')}</div><div class="alerts" style="margin-top:14px">${d.stops.filter(s=>s.pressure||s.fuel).map(s=>`<div class="alert ${s.pressure?'red':'gold'}"><strong>${s.emoji} ${esc(s.name)}</strong><br>${esc(s.pressure||s.fuel)}</div>`).join('')||'<div class="alert green">No special fuel/pressure action today.</div>'}</div>`} else if(state.activeTab==='stops'){ $('tabContent').innerHTML=`<div class="stop-list">${d.stops.map(stopCard).join('')}</div>` } else if(state.activeTab==='directions'){ if(route?.legs){$('tabContent').innerHTML=`${routeSketchSvg(d)}<div class="alert green"><strong>What to expect:</strong> ${esc(d.driveExperience?.summary||'')}</div>${heatherBadge(d)}<div class="directions">${route.legs.map((leg,li)=>`<h3>Leg ${li+1}: ${esc(leg.start)} → ${esc(leg.end)}</h3><p><strong>${esc(leg.distance)}</strong> · <strong>${esc(leg.duration)}</strong></p><ol>${leg.steps.map(s=>`<li>${esc(s.instruction)} <small>${esc(s.distance)} · ${esc(s.duration)}</small></li>`).join('')}</ol>`).join('')}</div>`}else{$('tabContent').innerHTML='<div class="alert gold">No cached Google Directions yet. Click <strong>Save key + render all</strong> while online, or use Google/Waze links.</div>'}} else if(state.activeTab==='street'){ if(route?.street?.length){$('tabContent').innerHTML=`<div class="street-grid">${route.street.map((s,i)=>`<div class="street-card"><img src="${s.url}" alt="Street View ${i+1}"><div><strong>${i+1}. ${esc(s.label)}</strong><br>${esc(s.lat.toFixed(5))}, ${esc(s.lng.toFixed(5))}</div></div>`).join('')}</div>`}else{$('tabContent').innerHTML='<div class="alert gold">Street View snapshots appear after Google Directions are rendered and the API key is available.</div>'}} else {$('tabContent').innerHTML='<div class="export-links"><a href="#" onclick="exportSelectedKml();return false;">Download selected day KML</a><a href="#" onclick="exportAllKmlZip();return false;">Download all day KML ZIP</a><a href="#" onclick="printMode();return false;">Print / Save PDF — one page per day</a></div>'} };

  renderPrintPages = function(){ $('printPages').innerHTML=DATA.days.map(d=>`<article class="print-day"><h1>Day ${d.day}: ${esc(d.title)}</h1><h2>${esc(d.date)} · ${d.selfDrive?'Self-drive / route day':'Guided or local day'}</h2>${routeSketchSvg(d,{print:true})}<div class="print-experience"><h3>What to expect on this leg</h3><p><strong>${esc(d.driveExperience?.summary||'')}</strong></p><ul>${(d.driveExperience?.expect||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p><strong>Watch for:</strong> ${(d.driveExperience?.hazards||[]).map(esc).join(' · ')}</p></div>${heatherPrintBlock(d)}<p>${esc(d.routeNotes)}</p><div class="print-grid">${d.stops.map(s=>`<div class="print-card"><h3>${esc(s.emoji+' '+s.time+' — '+s.name)}</h3><p><b>TYPE:</b> ${esc(s.type)} · <b>Route:</b> ${esc(s.routeRole)}</p><p>${esc(s.notes)}</p>${s.pressure?`<p><b>Tyre:</b> ${esc(s.pressure)}</p>`:''}${s.fuel?`<p><b>Fuel:</b> ${esc(s.fuel)}</p>`:''}<p><a href="${wazeUrl(s)}">Waze</a> · <a href="https://www.google.com/maps/search/?api=1&query=${s.lat},${s.lng}">Google Maps</a></p></div>`).join('')}</div></article>`).join(''); };

  const alerts=document.querySelector('.alerts'); if(alerts&&!alerts.textContent.includes('Heather driving indicator')) alerts.insertAdjacentHTML('beforeend','<div class="alert gold"><strong>Heather driving indicator:</strong> To reduce clutter, the app only labels specific segments Heather could plausibly drive. No Heather segment badge means David should own that leg/section.</div>');
  render();
})();
